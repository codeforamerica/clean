<form name="basic_info" action="/application/assister_info" method="post" class="form col-lg-6 col-lg-offset-3 col-sm-8 col-sm-offset-2 col-xs-10 col-xs-offset-1" data-parsley-validate>
  <h2 class="page-header">Do you have assistance today?</h2>
	<div class="form-group">
      <label for="primary_language" class="form-label">Your preferred language</label>
      <select class="form-control solo" name="primary_language">
        <% @language_options.each do |language| %>
          <option value="<%= language %>"><%= language %></option>
        <% end %>
      </select>
<!--     <label for="assister_org" class="form-label">Your preferred language</label>
    <select class="form-control solo" name="primary_language">
      <% @assister_options.each do |assister| %>
        <option value="<%= assister %>"><%= assister %></option>
      <% end %>
    </select> -->
  </div>
  <div class="page-footer">
    <div class="page-footer-content col-md-4 col-md-offset-8">
      <input class="btn next-step" type="submit" value="Next Step">
    </div>
  </div>
</form>

<script>
  $('.btn').button()
</script>