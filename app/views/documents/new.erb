<form name="first_id_doc" action="/documents/<%= @token %>/<%= @number_of_docs %>/create" method="post" enctype="multipart/form-data" class="form col-lg-6 col-lg-offset-3 col-sm-8 col-sm-offset-2 col-xs-10 col-xs-offset-1">
  <h2 class="page-header">Upload and send a document</h2>
  <div class="form-group">
  <p class="page-context">Take photos of key documents, then upload and submit them one at a time.</p>
  <p class="page-context">To be approved for CalFresh, you'll' need to send in documentation of at least a few things:
  <ul class="list">
    <li class="list-item"><strong>Identification (ID)</strong> for each person you cook and prepare food with (for example, a driver's license or passport)</li>
    <li class="list-item"><strong>Income</strong> (for example, a bank statement)</li>
    <li class="list-item"><strong>Expenses</strong> (for example, a rental lease and a utility bill)</li>
  </ul>
  <p class="page-context">If you don't have a document, you can write down your information and sign your name below to state it's true.</p>
  <label for="identification" class="form-label">Upload a document</label>
    <p class="form-context">(If you're on a phone, click the button to take a picture!)</p>
    <input type="file" name="identification" autofocus="autofocus" class="form-context">
    </input>
    <br>
  <div class="page-footer" style="display:none">
    <input class="btn next-step" type="submit" value="Send This Document">
  </div>
  </div>
</form>

<% if @number_of_docs.to_i > 0 %>
<form name="send" action="/documents/<%= @token %>/<%= @number_of_docs %>/submit" method="post" class="form col-lg-6 col-lg-offset-3 col-sm-8 col-sm-offset-2 col-xs-10 col-xs-offset-1">
  <div class="page-footer" id="done-partial">
    <h3 class="or" style="text-align:center">or</h5>
    <button id="submit_button" class="btn next-step" type="submit" value="I'm Done! Submit My Documents" data-loading-text="Sending in your documents...">I'm done for now.</button>
  </div>
</form>
<% end %>

<script>
  $('.btn').button();
  $(function() {
     $("input:file").change(function () {
       $(".page-footer").removeAttr("style");
       $(".or").hide();
       $(".skip").hide();
       $("#done-partial").hide();
     });
  });

  // Switch submit button to loading state on click
  $( "#submit_button" ).click(function() {
    $('#submit_button').button('loading');
  });
</script>
